# Write R script for some inbuilt functions like :
# help(),c(),ls(),rm(),sqrt(),seq(),min(),max(),assign(),print()

help() #display help
c(1:3) #make a vector of combine two
ls() #show all var in working directory by deafult all.names=FALSE
#if all.names=TRUE var begining with "." will be shown unhide vars
sqrt(9) #sqrt of number
seq(1:9) #buid sequence 
min(2,3) #find min between them
max(3,4) #find max between them

assign("x",2) #assign a var a value var is written as string
print(x) #print something or variable
